# ============================================================
# Panelin v4 — Agno Architecture Dependencies
# ============================================================

# Agno Framework (AI agent orchestration)
agno>=2.5.0
agno[openai]

# LLM Providers
openai>=1.50.0

# Alternative providers (optional — swap model without code changes)
# anthropic>=0.34.0
# google-generativeai>=0.8.0

# MCP Protocol (Model Context Protocol)
mcp>=1.0.0

# Web Framework
fastapi>=0.109.0
uvicorn[standard]>=0.27.0

# Database — PostgreSQL (Cloud SQL)
sqlalchemy[asyncio]>=2.0.25
psycopg>=3.1.0         # psycopg3 (asyncio-native, required by Agno PostgresDb)
asyncpg>=0.29.0        # alternative async driver

# Configuration
pydantic>=2.5.0
pydantic-settings>=2.1.0

# Google Cloud
google-cloud-storage>=2.10.0
google-auth>=2.27.0
google-api-python-client>=2.100.0

# Google Sheets CRM
gspread>=6.0.0

# PDF Generation
reportlab>=4.0.0
pillow>=10.0.0

# Utilities
python-dotenv>=1.0.0
httpx>=0.27.0

# Testing
pytest>=7.0.0
pytest-asyncio>=0.21.0
