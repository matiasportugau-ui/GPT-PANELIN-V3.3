# ─── Panelin Sheets Orchestrator – Environment Variables ───
# Copy to .env and fill in real values.  Never commit .env to git.

# Runtime environment (dev | staging | prod)
ENV=dev

# Google Cloud project
GCP_PROJECT_ID=<GCP_PROJECT_ID>

# OpenAI
OPENAI_API_KEY=<OPENAI_API_KEY>
OPENAI_MODEL=gpt-4o-mini

# Inbound API key (same pattern as Wolf API's X-API-Key)
PANELIN_ORCH_API_KEY=<PANELIN_ORCH_API_KEY>

# Google Sheets scopes (comma-separated)
SHEETS_SCOPES=https://www.googleapis.com/auth/spreadsheets

# Google service account JSON (base64-encoded) – for local dev only.
# On Cloud Run, use Application Default Credentials (ADC) instead.
# GOOGLE_SA_JSON_B64=

# Idempotency backend: "memory" (dev) or "firestore" (prod)
IDEMPOTENCY_BACKEND=memory
FIRESTORE_COLLECTION=panelin_sheet_jobs
FIRESTORE_DATABASE=(default)

# Template paths (relative to working directory)
TEMPLATES_DIR=templates/sheets
QUEUE_TEMPLATE_PATH=templates/queue/queue_v1.example.json

# Logging
LOG_LEVEL=INFO
