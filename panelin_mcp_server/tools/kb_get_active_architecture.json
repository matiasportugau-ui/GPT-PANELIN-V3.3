{
  "name": "kb_get_active_architecture",
  "description": "Retrieve the currently active KB architecture version with all modules. Returns version metadata, checksums, and full module JSONB data. No password required - read-only operation.",
  "inputSchema": {
    "type": "object",
    "properties": {
      "include_module_data": {
        "type": "boolean",
        "description": "Whether to include full module JSONB data in the response (default: true)",
        "default": true
      }
    },
    "required": []
  }
}
