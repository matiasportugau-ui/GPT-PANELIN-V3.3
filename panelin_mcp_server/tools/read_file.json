{
  "name": "read_file",
  "description": "Read the contents of a project file. No password required. Blocked: .env, .git/, credentials, binary files, paths outside project root.",
  "inputSchema": {
    "type": "object",
    "properties": {
      "file_path": {
        "type": "string",
        "description": "Relative path within the project (e.g., 'README.md', 'mcp/server.py'). No '..' allowed."
      }
    },
    "required": ["file_path"]
  }
}
